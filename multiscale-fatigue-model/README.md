# Important Note on Data Files

Due to GitHub's file size limits, the large dataset required for the real specimen simulation (e.g., the `.dat` stress field file) has been compressed. 

**Before running the simulation script, please ensure you extract the compressed archive (e.g., `.zip` or `.7z`) in this directory.** The script expects to read the uncompressed `.dat` and `.inp` files directly from this folder.
